[project]
name = "yurss"
version = "0.0.1"
requires-python = ">= 3.10"

[project.scripts]
yurss = "yurss.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["yurss"]
